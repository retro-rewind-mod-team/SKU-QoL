-- ============================================================
--  Retro Rewind - SKU QoL
--  CONFIGURATION FILE
--
--  Debug       = true → logs hook registrations and internal
--                        errors to the console
--
--  HookDelayMs = milliseconds to wait after load before
--                registering hooks (default: 3000)
--                Increase if hooks fail on slower systems.
-- ============================================================

return {

    Debug       = false,
    HookDelayMs = 3000,

}
